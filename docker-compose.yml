services:
  btm-ems-3:
    image: ${IMAGE_TAG:-ghcr.io/starenergyems/btm_ems_v3_front-end:local}
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - "3000:80"
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
        